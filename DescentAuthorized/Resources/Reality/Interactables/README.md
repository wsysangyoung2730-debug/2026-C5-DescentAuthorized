# Interactables

The current interactive entities are embedded in each floor scene so their authored
placement and door states remain intact. Resolve them using the `entityAliases` and
`runtimeDoorTransition` entries in `AssetManifest.json`.
